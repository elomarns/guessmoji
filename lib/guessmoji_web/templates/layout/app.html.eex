<!DOCTYPE html>

<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Guess the movie from the emoji!">
    <meta name="author" content="Elomar Nascimento dos Santos">

    <!-- Facebook metatags -->    
    <meta property="fb:app_id"       content="175326889855001">
    <meta property="og:url"          content="<%= current_url(@conn) %>">
    <meta property="og:type"         content="website">
    <meta property="og:title"        content="Guessmoji">
    <meta property="og:description"  content="Guess the movie from the emojis">
    <meta property="og:image"        content="<%= static_url(@conn, "/images/bg.png") %>">
    <meta property="og:image:width"  content="400">
    <meta property="og:image:height"  content="400">

    <title>Guessmoji - Guess the movie from the emoji!</title>

    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">

    <%= render "google_analytics.html" %>
  </head>

  <body>
    <%= render "header.html", assigns %>

    <div class="container">
      <%= alert(@conn, :info) %>
      <%= alert(@conn, :error) %>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>
    </div>

    <!-- EmojiOne doesn't work with brunch build --production, so it's being included here. -->
    <script src="<%= static_path(@conn, "/js/emojione.min.js") %>""></script>
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
